---
language: node_js
node_js: 11
cache:
  npm: true
  directories:
    - ~/.cache
addons:
  chrome: stable
  apt:
    packages:
      - libgconf-2-4
services:
  - xvfb
script:
  - npx run-p lint spellcheck test
after_success:
  - npx codecov
deploy:
  provider: script
  script: npx semantic-release
  skip_cleanup: true
  on:
    branch: master
